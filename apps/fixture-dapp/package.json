{
  "name": "@agent-browser-wallet/fixture-dapp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/fixture.js",
  "types": "./dist/fixture.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "serve": "python3 -m http.server 5173 --bind 127.0.0.1"
  },
  "devDependencies": {}
}
